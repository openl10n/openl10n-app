app.factory('TranslationService', function(Project) {
  var TranslationService = {};

  TranslationService.getTranslationCommits = function(projectSlug, sourceLocale, targetLocale) {
    return [
    ]
  };

  return TranslationService;
});
