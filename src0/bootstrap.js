// Set the Angular app attribute to the body.
// The ng-app attribute is required to init the Angular app.
// We don't add it directly to the index html page in order to make it
// indenpendent from the app.
// The index.html should have be an empty page and must only reference
// the stylesheets and the javascript sources.
//document.body.setAttribute('ng-app', 'app');
